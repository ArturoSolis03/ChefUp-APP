pipeline {
  agent any

  environment {
    FLY_API_TOKEN = credentials('FLY_API_TOKEN')
    PATH = "${HOME}/.fly/bin:${PATH}"
  }

  stages {
    stage('Clonar Repositorio') {
      steps {
        git 'https://github.com/ArturoSolis03/ChefUp-APP/tree/backend/backend'
      }
    }

    stage('Instalar Fly CLI') {
      steps {
        sh '''
          if ! command -v fly &> /dev/null; then
            curl -L https://fly.io/install.sh | sh
          fi
        '''
      }
    }

    stage('Deploy Auth Service') {
      steps {
        dir('auth-service') {
          sh '''
            export FLY_ACCESS_TOKEN=$FLY_API_TOKEN
            fly deploy --remote-only
          '''
        }
      }
    }

    stage('Deploy Favorites Service') {
      steps {
        dir('favorites-service') {
          sh '''
            export FLY_ACCESS_TOKEN=$FLY_API_TOKEN
            fly deploy --remote-only
          '''
        }
      }
    }
  }

  post {
    success {
      echo '✅ Microservicios desplegados con éxito en Fly.io.'
    }
    failure {
      echo '❌ Algo falló en el despliegue.'
    }
  }
}